# AI 核对与审核功能说明

## 新增功能

### 1. AI 核对项目信息（项目设置页面 - PENDING 状态）

**位置**：软件信息卡片右上角「AI 核对」按钮

**功能**：
- 核对用户手动填写的项目元数据（软件全称、版本号、开发目的、主要功能、运行平台等）是否合理
- 检查是否与仓库实际情况（目录结构、编程语言、描述）一致
- 识别前后矛盾、不符合软著规范的填写

**输出**：
- 整体评价（一句话总评）
- 问题列表，每个问题包含：
  - 字段名称（中文）
  - 严重程度（严重/中等/轻微）
  - 具体问题描述
  - 修改建议
  - 「应用」按钮：一键采纳建议并填入表单

**使用时机**：
- 自动检测完成后，生成材料前
- 需要先生成过一次材料（需要 reviewContext）

---

### 2. AI 审核说明书（项目详情页面 - DONE 状态）

**位置**：说明书文稿卡片下方独立卡片「审核说明书」按钮

**功能**：
- 检查生成的操作说明书是否存在幻觉/编造内容
- 验证文档描述的功能、模块、错误码是否与代码实际情况一致
- 评估是否符合软著文档鉴别材料规范
- 给出文档质量评分和初步通过概率

**输出**：
- **文档质量评分**：0-100 分
- **初步通过概率**：0-100%
- **总体评价**：2-3句话概括可用性和主要风险
- **合格项/亮点**：列表形式展示文档优点
- **发现的问题**：列表形式，每个问题包含：
  - 问题类别（幻觉/一致性/软著规范/完整性/其它）
  - 严重程度（严重/中等/轻微）
  - 位置（章节/小节描述）
  - 具体问题
  - 修改建议

**使用时机**：
- 说明书生成完成后
- 手动编辑文稿后想验证修改效果
- 需要先生成过一次材料（需要 reviewContext）

---

### 3. 强制清空重新生成（项目编辑表单）

**位置**：项目编辑表单底部（仅当未切换分支且已有生成材料时显示）

**功能**：
- 默认行为：只改软件名字/版本号时，自动同步到已生成的 Markdown 文稿，不清空
- 勾选此项：强制清空已生成的程序/文档材料，保存后需要重新生成
- 切换分支时自动清空（不受此选项影响）

**解决问题**：
- 用户想要"只改名字时同步文稿"（默认已实现）
- 用户有时也需要"强制清空重新生成"（现在可选）

---

## 技术实现

### 数据持久化

新增 `Project.reviewContext` 字段（localStorage 持久化）：
```typescript
reviewContext?: {
  fileTree: string;      // 前 50 个文件路径，换行符分隔
  languages: string;     // 前 10 种编程语言，逗号分隔
  codeSummary: string;   // 前 10 个文件头部摘要（每个 500 字符）
}
```

**填充时机**：生成材料时第 1 步（代码分析）完成后立即持久化

**用途**：页面刷新后仍可调用 AI 核对/审核，无需重新下载整个仓库

### AI 调用

两个新增 AI 函数，均使用 `callAILong`（支持长文本，max_tokens 8192）：

1. **`reviewProjectMeta`**
   - 输入：软件名、版本、元数据、仓库信息、目录结构、编程语言
   - 输出：JSON 格式的问题列表 + 总评
   - Prompt 包含软著合规规则（`SOFT_COPYRIGHT_COMPLIANCE_RULES`）

2. **`auditManualMarkdown`**
   - 输入：软件名、版本、元数据、目录结构、编程语言、代码摘要、说明书 Markdown
   - 输出：JSON 格式的评分、通过概率、问题列表、亮点列表
   - Prompt 重点检查幻觉/编造、一致性、软著规范

### JSON 解析增强

新增 `extractJsonObject` 函数：
- 容忍模型输出带有 markdown 代码围栏、前导文本、尾随说明
- 查找最外层 `{ ... }` 并平衡括号
- 处理字符串转义，避免 `"..."` 内的 `}` 误判

### UI 设计

采用 warm & earthy 配色（与设计规范一致）：
- 审核结果面板：`#F2E3D6` 背景，`#C4612F` 边框/强调色
- 严重问题：红色 `var(--color-error)`
- 中等问题：terracotta `#C4612F`
- 轻微问题：灰色 `var(--color-muted)`
- 已应用建议：绿色 `var(--color-success)` + 60% opacity

---

## 使用流程

### 典型工作流 A：生成前核对

1. 创建项目，自动检测元数据
2. 点击「AI 核对」检查填写是否合理
3. 对于每个问题，点「应用」或手动修改
4. 确认信息并开始生成

### 典型工作流 B：生成后审核

1. 材料生成完成（DONE 状态）
2. 点击「审核说明书」查看文档质量
3. 如果发现严重问题（幻觉/编造），点「编辑文稿」修改
4. 修改后点「重新导出 PDF」
5. 可再次「审核说明书」验证修改效果

---

## 注意事项

1. **首次使用必须先生成一次材料**，才能使用核对/审核功能（需要 reviewContext）
2. **AI 审核结果仅供参考**，不能替代人工终审和真实软著审核
3. **修改建议需要用户判断**，不要盲目一键应用所有建议
4. **审核说明书会截断过长文档**（24000 字符）以适应模型上下文窗口
5. **核对/审核需消耗 AI tokens**（单次约 8k tokens output），请合理使用

---

## 文件变更

- `src/lib/ai-helpers.ts`：新增 5 个导出（reviewProjectMeta, auditManualMarkdown, MetaReviewResult, ManualAuditResult, 及辅助类型）
- `src/lib/storage.ts`：`Project` 类型新增 `reviewContext?` 字段
- `src/app/projects/[id]/page.tsx`：新增状态、handlers、UI 组件

所有改动向后兼容，已有项目无 `reviewContext` 时会提示"请先生成一次材料"。
