/**
 * App version, displayed in the UI footer/header.
 *
 * PATCH is bumped automatically by the `.githooks/pre-commit` hook on every
 * commit (see `npm run bump:patch`). Edit MAJOR/MINOR by hand when the release
 * warrants it.
 */
export const APP_VERSION = "0.1.3";
